'use strict';

/**
 * platform-material service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::platform-material.platform-material');
