'use strict';

/**
 * platform-one-task service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::platform-one-task.platform-one-task');
